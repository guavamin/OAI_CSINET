# Fragment: add a `cells` block to your UE libconfig (or merge with -O if your build supports it).
# Pair with gnb0.prs.band261.fr2.64PRB.usrpx310.conf: band 261, 120 kHz, 66 PRB, PointA 2070979 / SSB 2071387.
#
# rf_freq MUST match the gNB DL carrier center (same value as nr-softmodem -C), in Hz.
# ssb_start = 84 matches ssb_SubcarrierOffset=0 and the configured ARFCNs (nr_get_ssb_start_sc path).
#
cells = (
  {
    ru_id = 0;
    band = 261;
    rf_freq = 27556320000L;
    rf_offset = 0;
    numerology = 3;
    N_RB_DL = 66;
    ssb_start = 84;
  }
);
